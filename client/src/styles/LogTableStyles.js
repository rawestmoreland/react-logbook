import { makeStyles } from '@material-ui/core/styles';

export const logTableStyles = makeStyles((theme) => ({
	root: {
		width: '100%',
	},
	container: {
		maxHeight: '85vh',
	},
	topHeader: {
		height: 80,
	},
}));
